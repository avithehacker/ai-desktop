{
  "name": "ramanujan-cli",
  "version": "1.0.0",
  "description": "Ramanujan CLI — prompt AI from anywhere in your terminal",
  "main": "dist/index.js",
  "bin": { "ram": "dist/index.js" },
  "files": ["dist"],
  "repository": { "type": "git", "url": "https://github.com/avithehacker/ai-desktop" },
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/index.ts",
    "package": "npm run build && pkg dist/index.js --targets node20-macos-arm64,node20-win-x64,node20-linux-x64 --output-path binaries/ --compress GZip"
  },
  "pkg": {
    "assets": [],
    "outputPath": "binaries"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "ts-node": "^10.9.0",
    "pkg": "^5.8.1"
  }
}
